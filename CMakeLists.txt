CMAKE_MINIMUM_REQUIRED(VERSION 2.8.3 FATAL_ERROR)
PROJECT(PeakInvestigator-API-CPP)

FIND_PACKAGE(CURL)

include(include/headers.cmake)

ADD_SUBDIRECTORY(include)
ADD_SUBDIRECTORY(src)
ADD_SUBDIRECTORY(test)

SET(HEADERS)
FOREACH(HEADER ${PEAKINVESTIGATOR_HEADERS})
  LIST(APPEND HEADERS "include/${HEADER}")
ENDFOREACH(HEADER)

INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/include
                    ${PROJECT_SOURCE_DIR}/include/jsoncpp)

#ADD_EXECUTABLE(Test test.cpp ${HEADERS})
#TARGET_LINK_LIBRARIES(Test PeakInvestigatorSaaS)
